include ./variables.pug

.hero__links
  ul.socials
    each item in socials
      li.socials__item
        a(href=`${item[0]}`).socials__link
          +icon(item[1], 'socials__link-icon')
.hero__menu
    a(href="").hamburger

+nav(hero__nav, ["Мои работы","Обо мне","Блог","Авторизация"], 1)
    